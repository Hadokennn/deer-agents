# config.yaml — deer-agents 自身配置
# 模型、tools、memory 等 deer-flow 配置在 deer-flow/config.yaml 中维护

default_agent: oncall

checkpointer:
  type: sqlite
  path: .deer-flow/checkpoints.db

sessions:
  dir: .deer-flow/sessions/
